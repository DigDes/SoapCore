name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
   
    steps:
    - uses: actions/checkout@v1
    - name: build and test
      uses: docker://mcr.microsoft.com/dotnet/core/sdk:3.0-alpine
    - name: Run a multi-line script
      run: |
        cat /etc/os_release
    container:
      volumes:
          - src:/src
